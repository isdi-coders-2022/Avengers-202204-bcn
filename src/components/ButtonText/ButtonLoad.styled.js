import styled from "styled-components";

const StyledButtonLoad = styled.button`
  border: 1px solid black;
  border-radius: 0.8rem;
  margin-top: 2rem;
  font-size: 4vw;

  :active {
    background-color: black;
    color: #fff;
  }

  :hover {
    cursor: pointer;
  }
`;

export default StyledButtonLoad;
